# SPA fallback is handled in wrangler.toml:
#   [assets] not_found_handling = "single-page-application"
# Do NOT use "/* /index.html 200" here — Cloudflare flags it as an
# infinite loop and ignores the rule (breaks client-side routes).
