# Optional local/Pages config. Workers AI binding can also be set in the Cloudflare dashboard:
# Pages → Settings → Functions → Bindings → Workers AI → variable name "AI"
name = "family-command-centre"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"

[ai]
binding = "AI"
